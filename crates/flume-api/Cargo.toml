[package]
name = "flume-api"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
flume-core = { path = "../flume-core", version = "0.1.0" }
flume-runtime = { path = "../flume-runtime", version = "0.1.0" }
tokio = { version = "1", features = ["sync", "rt", "macros"] }
